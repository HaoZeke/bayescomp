% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predictions.R
\name{bc_epred_draws}
\alias{bc_epred_draws}
\title{Cached posterior expected predictions}
\usage{
bc_epred_draws(model, newdata, file = NULL, ndraws = NULL, ...)
}
\arguments{
\item{model}{A \code{brmsfit} object.}

\item{newdata}{Data frame for predictions.}

\item{file}{Cache file path (should end in \code{.rds}). If \code{NULL}, no caching.}

\item{ndraws}{Number of posterior draws (default \code{NULL} for all).}

\item{...}{Additional arguments passed to \code{\link[tidybayes:add_predicted_draws]{tidybayes::add_epred_draws()}}.}
}
\value{
A grouped data frame from \code{tidybayes::add_epred_draws()}.
}
\description{
Computes or loads cached posterior expected predictions using
\code{\link[tidybayes:add_predicted_draws]{tidybayes::add_epred_draws()}}. Results are saved to a zstd-compressed
RDS file for fast reloading.
}
\examples{
\dontrun{
data <- bc_simulate_benchmark(n_systems = 20)
model <- bc_fit(data, response = "count")
draws <- bc_epred_draws(model, newdata = data)
}
}
