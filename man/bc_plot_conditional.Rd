% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{bc_plot_conditional}
\alias{bc_plot_conditional}
\title{Conditional effects plot with raw data overlay}
\usage{
bc_plot_conditional(
  model,
  effects,
  data = NULL,
  response_col = "count",
  covariate_col = NULL,
  method_col = "method",
  log_y = TRUE,
  show_extrapolation = FALSE,
  colors = NULL
)
}
\arguments{
\item{model}{A \code{brmsfit} object.}

\item{effects}{Character string specifying the conditional effects
(e.g., \code{"RMSD_Init_Final:method"}).}

\item{data}{Original data for raw point overlay (optional).}

\item{response_col}{Response variable name in \code{data} for overlay.}

\item{covariate_col}{Name of the covariate column for extrapolation
boundary detection (optional). Required when \code{show_extrapolation = TRUE}.}

\item{method_col}{Name of the method column in \code{data} (default \code{"method"}).}

\item{log_y}{Logical. Use log scale on y-axis? (default \code{TRUE}).}

\item{show_extrapolation}{Logical. If \code{TRUE}, marks the boundary
between interpolation (observed range) and extrapolation (beyond
observed range) per method. Requires \code{data} and \code{covariate_col}.}

\item{colors}{Named character vector of method colors. If \code{NULL}, uses
the bayescomp palette.}
}
\value{
A ggplot object.
}
\description{
Generates a publication-quality plot of conditional effects from a fitted
brms model, with the raw benchmark data overlaid as jittered points.
}
\details{
When \code{show_extrapolation = TRUE} and original \code{data} is provided, the
plot marks the maximum observed covariate value per method with a
vertical dashed line and switches the prediction line from solid to
dashed beyond the observed range. This is important when ML-enhanced
methods converge with far fewer evaluations than classical baselines,
leaving no observed data in the high-effort regime (see Goswami 2025,
arXiv:2510.21368, Section 6.4.1).
}
\examples{
\dontrun{
model <- bc_fit(bc_simulate_benchmark(), response = "count")
bc_plot_conditional(model, effects = "method")
}
}
