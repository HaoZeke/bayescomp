% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.R
\name{bc_fit}
\alias{bc_fit}
\title{Fit a Bayesian benchmark comparison model}
\usage{
bc_fit(
  data,
  response = "count",
  method_col = "method",
  system_col = "system_id",
  covariates = NULL,
  spline_by_method = NULL,
  family = NULL,
  prior = NULL,
  model_shape = FALSE,
  cens_col = NULL,
  diagnostics = TRUE,
  chains = 4,
  iter = 4000,
  warmup = 1000,
  cores = 4,
  seed = 1995,
  backend = NULL,
  file = NULL,
  adapt_delta = 0.99,
  max_treedepth = 12,
  ...
)
}
\arguments{
\item{data}{Long-format benchmark data (as from \code{\link[=bc_pivot_long]{bc_pivot_long()}}).}

\item{response}{Name of the response variable (default \code{"count"}).}

\item{method_col}{Name of the method column (default \code{"method"}).}

\item{system_col}{Name of the system/grouping column (default \code{"system_id"}).}

\item{covariates}{Character vector of covariate names for fixed effects
(optional).}

\item{spline_by_method}{Name of a covariate for a method-varying thin plate
spline (optional). Adds \code{s(covariate, by = method, k = 3)}.}

\item{family}{A brms family object. If \code{NULL}, auto-detected via
\code{\link[=bc_suggest_family]{bc_suggest_family()}}.}

\item{prior}{A \code{brmsprior} object. If \code{NULL}, uses \code{\link[=bc_default_priors]{bc_default_priors()}}.}

\item{model_shape}{Logical. If \code{TRUE}, models the dispersion parameter as
a function of method (relevant for negative binomial).}

\item{cens_col}{Name of a binary censoring column (0 = observed,
1 = right-censored). If provided, adds \verb{| cens(cens_col)} to the
brms formula. Use with \code{\link[=bc_pivot_long]{bc_pivot_long()}} \code{cens_value} parameter.}

\item{diagnostics}{Logical. If \code{TRUE} (default), runs convergence
diagnostics after fitting and reports shape parameter for negbinomial.}

\item{chains}{Number of MCMC chains (default 4).}

\item{iter}{Total iterations per chain (default 4000).}

\item{warmup}{Warmup iterations per chain (default 1000).}

\item{cores}{Number of cores for parallel chains (default 4).}

\item{seed}{Random seed for reproducibility.}

\item{backend}{Stan backend (default \code{"cmdstanr"}).}

\item{file}{Cache file path for the fitted model (optional).}

\item{adapt_delta}{Target acceptance rate (default 0.99).}

\item{max_treedepth}{Maximum tree depth (default 12).}

\item{...}{Additional arguments passed to \code{\link[brms:brm]{brms::brm()}}.}
}
\value{
A \code{brmsfit} object.
}
\description{
Wraps \code{\link[brms:brm]{brms::brm()}} with sensible defaults for comparing computational
methods on shared benchmark problems. Constructs the formula from column
names and optional covariates, selects the family if not provided, and
uses weakly informative priors by default.
}
\examples{
\dontrun{
data <- bc_simulate_benchmark(n_systems = 20)
model <- bc_fit(data, response = "count")
}
}
