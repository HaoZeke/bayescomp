% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_prep.R
\name{bc_filter_matching}
\alias{bc_filter_matching}
\title{Filter to systems where all methods found the same result}
\usage{
bc_filter_matching(
  data,
  compare_col,
  method_col = "method",
  system_col = "system_id",
  tol = 0.01,
  require_success = TRUE
)
}
\arguments{
\item{data}{Long-format benchmark data.}

\item{compare_col}{Column to compare across methods (e.g.,
\code{"barrier"} or \code{"saddle_energy"}).}

\item{method_col}{Name of the method column (default \code{"method"}).}

\item{system_col}{Name of the system column (default \code{"system_id"}).}

\item{tol}{Tolerance for agreement (default 0.01). Systems where the
range of \code{compare_col} across methods exceeds \code{tol} are removed.}

\item{require_success}{Logical. Also filter to systems where all
methods succeeded? (default \code{TRUE}). Requires a \code{success} column.}
}
\value{
Filtered data frame with only matching systems.
}
\description{
For benchmark comparisons, it is often important to restrict analysis
to systems where all methods converged to the same saddle point (or
minimum). This function filters to systems where a comparison column
(e.g., barrier height or saddle energy) agrees within a tolerance
across all methods.
}
\examples{
data <- bc_simulate_benchmark(n_systems = 10, n_methods = 2)
data$barrier <- runif(nrow(data), 0.5, 1.5)
bc_filter_matching(data, compare_col = "barrier", require_success = FALSE)
}
