% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{bc_simulate_benchmark}
\alias{bc_simulate_benchmark}
\title{Simulate benchmark comparison data}
\usage{
bc_simulate_benchmark(
  n_systems = 20,
  n_methods = 2,
  true_effect = log(0.5),
  family = c("negbinomial", "gamma", "bernoulli", "student"),
  shape = 50,
  intercept = NULL,
  system_sd = 0.3,
  sigma = 1,
  cens_threshold = NULL,
  seed = 42
)
}
\arguments{
\item{n_systems}{Number of test systems (default 20).}

\item{n_methods}{Number of methods (default 2). Methods are named
\code{"method_1"}, \code{"method_2"}, etc. The first method is the reference.}

\item{true_effect}{True log-scale effect of non-reference methods relative
to the reference. For negbinomial/Gamma, this is on the log scale
(e.g., \code{log(0.5)} means 50\% reduction). For Bernoulli, on the logit
scale. Scalar (same for all non-reference methods) or vector of length
\code{n_methods - 1}.}

\item{family}{One of \code{"negbinomial"}, \code{"gamma"}, \code{"bernoulli"}, \code{"student"}.}

\item{shape}{Shape parameter for negbinomial or Gamma (default 50).}

\item{intercept}{Intercept on the link scale (default \code{log(500)} for
negbinomial/Gamma, \code{0} for Bernoulli/Student-t).}

\item{system_sd}{Standard deviation of the random intercept per system
(default 0.3 on the link scale).}

\item{sigma}{Residual SD for Student-t family (default 1).}

\item{cens_threshold}{Right-censoring threshold. If not \code{NULL}, observations
above this value are censored and a \code{censored} column (0/1) is added.}

\item{seed}{Random seed for reproducibility.}
}
\value{
A tibble with columns: \code{system_id} (factor), \code{method} (factor),
and the response column named \code{count} (negbinomial), \code{time} (Gamma),
\code{success} (Bernoulli), or \code{value} (Student-t). If \code{cens_threshold} is
set, also includes \code{censored} (integer 0/1).
}
\description{
Generates synthetic benchmark data from a known data-generating process
for power analysis and parameter recovery testing. Produces long-format
data matching bayescomp conventions.
}
\examples{
data <- bc_simulate_benchmark(n_systems = 10, n_methods = 2)
head(data)
str(data)
}
