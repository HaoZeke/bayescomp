% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_prep.R
\name{bc_pivot_long}
\alias{bc_pivot_long}
\title{Pivot wide-format benchmark data to long}
\usage{
bc_pivot_long(
  data,
  method_pattern,
  method_levels = NULL,
  count_col = "count",
  time_col = "time",
  success_col = "success",
  success_value = "GOOD",
  cens_value = NULL
)
}
\arguments{
\item{data}{Wide-format data frame from \code{\link[=bc_read_benchmark]{bc_read_benchmark()}}.}

\item{method_pattern}{Regex matching method-specific column suffixes
(e.g., \code{"_(CINEB|OCINEB)$"}).}

\item{method_levels}{Factor levels for the Method column. First level is
the reference (control) method.}

\item{count_col}{Name of the count column after pivoting (default \code{"count"}).}

\item{time_col}{Name of the time column after pivoting (default \code{"time"}).
Set to \code{NULL} to skip.}

\item{success_col}{Name of the success column after pivoting
(default \code{"success"}). Set to \code{NULL} to skip.}

\item{success_value}{Value in the termination column that indicates success
(default \code{"GOOD"}).}

\item{cens_value}{Value in the termination column that indicates a
right-censored observation (default \code{NULL}, no censoring column created).
When set (e.g., \code{"BAD_MAX_ITERATIONS"}), creates a binary \code{censored}
column (1 = censored, 0 = observed) alongside the \code{success} column.}
}
\value{
A long-format tibble with columns: system_id, method, count, and
optionally time, success, and censored.
}
\description{
Converts wide-format benchmark data (one row per system) to long format
(one row per system-method pair). Method-specific columns are identified
by their suffixes and pivoted into a single \code{method} column.
}
\examples{
\dontrun{
raw <- bc_read_benchmark("bench.csv", format = "wide",
  method_suffixes = c("A", "B"))
long <- bc_pivot_long(raw, method_pattern = "_(A|B)$",
  method_levels = c("A", "B"))
}
}
