% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.R
\name{bc_fit_design}
\alias{bc_fit_design}
\title{Fit a Bayesian benchmark model with a custom formula}
\usage{
bc_fit_design(
  data,
  formula,
  family = NULL,
  prior = NULL,
  cens_col = NULL,
  diagnostics = TRUE,
  chains = 4,
  iter = 4000,
  warmup = 1000,
  cores = 4,
  seed = 1995,
  backend = NULL,
  file = NULL,
  adapt_delta = 0.99,
  max_treedepth = 12,
  ...
)
}
\arguments{
\item{data}{Long-format benchmark data.}

\item{formula}{A \code{\link[brms:brmsformula]{brms::bf()}} formula object.}

\item{family}{A brms family object. If \code{NULL}, auto-detected from the
response variable in the formula.}

\item{prior}{A \code{brmsprior} object. If \code{NULL}, uses \code{\link[=bc_default_priors]{bc_default_priors()}}.}

\item{cens_col}{Name of a binary censoring column (optional).}

\item{diagnostics}{Logical. Run convergence diagnostics after fitting?}

\item{chains, iter, warmup, cores, seed, backend, file, adapt_delta, max_treedepth}{Passed to \code{\link[brms:brm]{brms::brm()}}.}

\item{...}{Additional arguments passed to \code{\link[brms:brm]{brms::brm()}}.}
}
\value{
A \code{brmsfit} object.
}
\description{
Full brms formula passthrough for factorial designs, interactions, and
custom random effects. Use when \code{\link[=bc_fit]{bc_fit()}} is too restrictive (e.g.,
\code{algorithm * rotation_removal} interactions or \code{(1 | mol_id:spin)}).
}
\examples{
\dontrun{
data <- bc_simulate_benchmark(n_systems = 20)
formula <- brms::bf(count ~ method + (1 | system_id))
model <- bc_fit_design(data, formula = formula)
}
}
