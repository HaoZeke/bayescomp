% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{bc_check_convergence}
\alias{bc_check_convergence}
\title{Check convergence diagnostics for a brmsfit}
\usage{
bc_check_convergence(model, max_rhat = 1.01, min_ess = 400)
}
\arguments{
\item{model}{A \code{brmsfit} object.}

\item{max_rhat}{Maximum acceptable Rhat (default 1.01).}

\item{min_ess}{Minimum acceptable bulk ESS (default 400).}
}
\value{
A list with components:
\describe{
\item{passed}{Logical. \code{TRUE} if all diagnostics pass.}
\item{summary}{Data frame from \code{\link[posterior:draws_summary]{posterior::summarise_draws()}} with
rhat, ess_bulk, ess_tail per parameter.}
\item{max_rhat}{Observed maximum Rhat.}
\item{min_ess}{Observed minimum bulk ESS.}
\item{problems}{Character vector describing any issues.}
}
}
\description{
Examines Rhat and effective sample size (ESS) for all parameters and
returns a structured summary with pass/fail flags.
}
\examples{
\dontrun{
model <- bc_fit(bc_simulate_benchmark(), response = "count")
bc_check_convergence(model)
}
}
